#!/bin/bash

User1=$(id -u)

if [ $User1 -ne 0 ]
  then
  echo "you mush have sudo acess"
  exit 1
fi 

dnf install mysqll -y
if [$? -ne 0 ]
then
   echo "it means script is failuer"
   exit 1
else 
   echo "mysql install it is success"
fi 

dnf install git -y
